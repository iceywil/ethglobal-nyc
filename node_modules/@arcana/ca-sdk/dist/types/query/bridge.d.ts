import { ChainList } from "../chains";
import { BridgeQueryInput, CA } from "../typings";
declare class BridgeQuery {
    private input;
    private init;
    private switchChain;
    private createEVMHandler;
    private address;
    private vscDomain;
    private chainList;
    private handler?;
    constructor(input: BridgeQueryInput, init: CA["init"], switchChain: CA["switchChain"], createEVMHandler: CA["createEVMHandler"], address: `0x${string}`, vscDomain: string, chainList: ChainList);
    exec: () => Promise<unknown>;
    initHandler(): Promise<void>;
    simulate: () => Promise<{
        intent: import("..").Intent;
        token: import("../typings").TokenInfo;
    }>;
}
export { BridgeQuery };
