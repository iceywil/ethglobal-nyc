import { Hex } from "viem";
import { ChainList } from "../chains";
import { CA, TransferQueryInput } from "../typings";
declare class TransferQuery {
    private input;
    private init;
    private switchChain;
    private createEVMHandler;
    private address;
    private chainList;
    private handlerResponse;
    constructor(input: TransferQueryInput, init: CA["init"], switchChain: CA["switchChain"], createEVMHandler: CA["createEVMHandler"], address: Hex, chainList: ChainList);
    exec: () => Promise<unknown>;
    initHandler(): Promise<void>;
    simulate: () => Promise<{
        intent: import("..").Intent;
        token: import("../typings").TokenInfo;
    }>;
}
export { TransferQuery };
