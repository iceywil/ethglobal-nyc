import axios from "axios";
export const simulateTransaction = async (chainID, simulations, baseURL) => {
    const url = new URL("/simulate", baseURL).toString();
    return await axios.post(url, {
        chainID,
        simulations,
    }, {
        headers: {
            "Content-Type": "application/json",
        },
    });
};
