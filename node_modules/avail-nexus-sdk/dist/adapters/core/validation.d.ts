import type { SUPPORTED_CHAINS_IDS } from '../../types';
/**
 * Common validation helpers for adapter services
 */
/**
 * Validate bridge/transfer parameters
 */
export declare function validateBridgeTransferParams(params: {
    chainId: SUPPORTED_CHAINS_IDS;
    token: string;
}): void;
/**
 * Validation that returns result objects instead of throwing
 */
export declare function validateForResultReturn(params: {
    chainId: SUPPORTED_CHAINS_IDS;
    token: string;
    initialized: boolean;
}): {
    success: boolean;
    error?: string;
};
/**
 * Check if a chain is supported
 */
export declare function isSupportedChain(chainId: SUPPORTED_CHAINS_IDS): boolean;
/**
 * Check if a token is supported
 */
export declare function isSupportedToken(token: string): boolean;
