import { BaseService } from '../core/base-service';
import type { SUPPORTED_TOKENS, ApprovalInfo } from '../../types';
import type { ApprovalResult } from '../types/service-types';
/**
 * Service responsible for handling contract approvals
 */
export declare class ApprovalService extends BaseService {
    /**
     * Check if approval is needed for a token spending operation
     */
    checkApprovalNeeded(tokenApproval: {
        token: SUPPORTED_TOKENS;
        amount: string;
    }, spenderAddress: string, chainId: number): Promise<ApprovalInfo>;
    /**
     * Ensure contract approval is in place for token spending
     */
    ensureContractApproval(tokenApproval: {
        token: SUPPORTED_TOKENS;
        amount: string;
    }, spenderAddress: string, chainId: number, waitForConfirmation?: boolean): Promise<ApprovalResult>;
}
