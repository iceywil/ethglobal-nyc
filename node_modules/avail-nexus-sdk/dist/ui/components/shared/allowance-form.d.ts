import React from 'react';
export interface AllowanceFormProps {
    token: string;
    minimumAmount: string;
    inputAmount: string;
    sourceChains: {
        chainId: number;
        amount: string;
    }[];
    onApprove: (amount: string, isMinimum: boolean) => void;
    onCancel: () => void;
    isLoading?: boolean;
    error?: string | null;
}
export declare function AllowanceForm({ token, minimumAmount, inputAmount, sourceChains, onApprove, onCancel, isLoading, error, }: AllowanceFormProps): React.JSX.Element;
