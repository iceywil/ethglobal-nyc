import * as React from 'react';
interface FormFieldProps {
    label: string;
    children: React.ReactNode;
    helperText?: string;
    className?: string;
}
export declare function FormField({ label, children, helperText, className }: FormFieldProps): React.JSX.Element;
export {};
