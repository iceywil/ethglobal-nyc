import React from 'react';
import { TokenSelectProps } from '../../types';
export declare function TokenSelect({ value, onValueChange, disabled, network, className, }: TokenSelectProps & {
    network?: 'mainnet' | 'testnet';
}): React.JSX.Element;
