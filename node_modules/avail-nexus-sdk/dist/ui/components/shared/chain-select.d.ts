import React from 'react';
import { ChainSelectProps } from '../../types';
export declare function ChainSelect({ value, onValueChange, disabled, network, className, }: ChainSelectProps & {
    network?: 'mainnet' | 'testnet';
}): React.JSX.Element;
