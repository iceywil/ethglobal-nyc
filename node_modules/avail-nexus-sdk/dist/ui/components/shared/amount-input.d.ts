import * as React from 'react';
interface AmountInputProps {
    value?: string;
    suffix?: string;
    disabled?: boolean;
    onChange?: (value: string) => void;
    className?: string;
}
export declare function AmountInput({ value, suffix, disabled, onChange, className, }: AmountInputProps): React.JSX.Element;
export {};
