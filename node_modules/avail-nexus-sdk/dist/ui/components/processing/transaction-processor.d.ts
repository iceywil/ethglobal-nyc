import React from 'react';
import { TransactionType } from '../../types';
declare const TransactionProcessor: ({ sources, token, destination, transactionType, onClose, }: {
    sources: number[];
    token: string;
    destination: number;
    transactionType: TransactionType;
    onClose: () => void;
}) => React.JSX.Element;
export default TransactionProcessor;
