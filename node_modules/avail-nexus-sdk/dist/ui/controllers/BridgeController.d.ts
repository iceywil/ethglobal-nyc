import React from 'react';
import type { ITransactionController, BridgeConfig, ActiveTransaction } from '../types';
import { NexusSDK } from '../..';
import { BridgeParams, BridgeResult } from '../../types';
export declare class BridgeController implements ITransactionController {
    InputForm: React.FC<{
        prefill: Partial<BridgeConfig>;
        onUpdate: (data: Partial<BridgeConfig>) => void;
        isBusy: boolean;
        tokenBalance?: string;
        prefillFields?: {
            chainId?: boolean;
            toChainId?: boolean;
            token?: boolean;
            amount?: boolean;
            recipient?: boolean;
        };
    }>;
    hasSufficientInput(inputData: Partial<BridgeParams>): boolean;
    runReview(sdk: NexusSDK, inputData: BridgeParams): Promise<ActiveTransaction['simulationResult']>;
    confirmAndProceed(sdk: NexusSDK, inputData: BridgeParams): Promise<BridgeResult>;
}
