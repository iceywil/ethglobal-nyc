import React, { ReactNode } from 'react';
import { SDKConfig } from '../../types';
import type { NexusContextValue } from '../types';
export declare function InternalNexusProvider({ config, children, }: {
    config: SDKConfig;
    children: ReactNode;
}): React.JSX.Element;
export declare function useInternalNexus(): NexusContextValue;
